{
  "name": "youtube-extended-title",
  "version": "0.1.0",
  "private": true,
  "description": "Extends YouTube browser tab titles with video metadata.",
  "scripts": {
    "clean": "node scripts/clean-dist.mjs",
    "build": "npm run clean && tsc && node scripts/copy-assets.mjs",
    "pack": "npm run build && node scripts/pack-dist.mjs",
    "watch": "tsc -w"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
